import styles from '@/styles/styles.module.css'

export default function Page(): React.ReactElement {
    return (
        <p className={styles.maintext}>A neural network based FRC match predictor.</p>
    )
}
